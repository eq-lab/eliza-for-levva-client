/**
 * Confidence threshold for intent detection
 * Requires 75% confidence before starting transaction flows
 * Higher threshold = more conservative (fewer false positives)
 */
export const INTENT_CONFIDENCE_THRESHOLD = 0.75;
